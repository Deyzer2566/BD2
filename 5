SELECT DISTINCT ГРУППА as Группа, kek.min AS Средний_возраст FROM Н_УЧЕНИКИ CROSS JOIN 
	(SELECT MIN(DATE_PART('year',AGE(now(),ДАТА_РОЖДЕНИЯ))) FROM
		Н_УЧЕНИКИ INNER JOIN Н_ЛЮДИ ON Н_УЧЕНИКИ.ЧЛВК_ИД = Н_ЛЮДИ.ИД
		WHERE ГРУППА = '3100'
	) AS kek;
